% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/file_management.R
\name{detectFastqFiles}
\alias{detectFastqFiles}
\title{Detect and organize FASTQ files}
\usage{
detectFastqFiles(
  input_dir,
  pattern = ".fastq|.fq",
  paired = TRUE,
  forward_pattern = "_R1",
  reverse_pattern = "_R2",
  sample_pattern = "(.+?)(_S[0-9]+)?(_L[0-9]+)?(_R[12])",
  recursive = FALSE
)
}
\arguments{
\item{input_dir}{Character string specifying the directory containing FASTQ files}

\item{pattern}{Character string specifying a pattern to match in file names (default: ".fastq|.fq")}

\item{paired}{Logical indicating whether to look for paired-end files (default: TRUE)}

\item{forward_pattern}{Character string pattern to identify forward reads (default: "_R1")}

\item{reverse_pattern}{Character string pattern to identify reverse reads (default: "_R2")}

\item{sample_pattern}{Regular expression to extract sample names (default: "(.+?)(_S\link{0-9}+)?(_L\link{0-9}+)?(_R\link{12})")}

\item{recursive}{Logical indicating whether to search directories recursively (default: FALSE)}
}
\value{
A tibble with columns for sample names, forward read paths, reverse read paths (if paired),
and file sizes
}
\description{
This function scans a directory for FASTQ files and organizes them into a structured
format for downstream analysis. It can detect paired-end files based on naming patterns.
}
\examples{
\dontrun{
# For paired-end files
fastq_files <- detectFastqFiles("path/to/fastq/dir")

# For single-end files
fastq_files <- detectFastqFiles("path/to/fastq/dir", paired = FALSE)
}
}
