% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filtering.R
\name{filterAndTrimReads}
\alias{filterAndTrimReads}
\title{Filter and trim sequences based on quality metrics}
\usage{
filterAndTrimReads(
  fastq_files,
  output_dir,
  truncLen = c(0, 0),
  maxEE = c(2, 2),
  truncQ = 2,
  maxN = 0,
  rm.phix = TRUE,
  compress = TRUE,
  multithread = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{fastq_files}{Tibble with FASTQ file information as produced by detectFastqFiles()}

\item{output_dir}{Character string specifying output directory for filtered files}

\item{truncLen}{Integer vector of length 2 giving truncation lengths for forward and reverse reads}

\item{maxEE}{Double vector of length 2 giving maximum expected errors for forward and reverse reads}

\item{truncQ}{Integer giving truncation quality threshold}

\item{maxN}{Integer giving maximum number of N bases allowed}

\item{rm.phix}{Logical indicating whether to remove PhiX reads}

\item{compress}{Logical indicating whether to gzip compress output files}

\item{multithread}{Logical or Integer indicating number of threads to use for processing}

\item{verbose}{Logical indicating whether to print verbose output}
}
\value{
A tibble with paths to filtered files and filtering statistics
}
\description{
This function filters and trims sequences based on quality metrics using DADA2's
filterAndTrim function.
}
\examples{
\dontrun{
fastq_files <- detectFastqFiles("path/to/fastq/dir")
filtered_files <- filterAndTrimReads(fastq_files, "path/to/filtered/dir",
                                   truncLen = c(240, 200), maxEE = c(2, 2))
}
}
