% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dada2_wrapper.R
\name{runDADA2Pipeline}
\alias{runDADA2Pipeline}
\title{Run complete DADA2 pipeline}
\usage{
runDADA2Pipeline(
  filtered_files,
  n_reads = 1e+06,
  pool = FALSE,
  min_length = 0,
  max_length = Inf,
  remove_chimeras = TRUE,
  multithread = FALSE,
  verbose = TRUE,
  paired = TRUE
)
}
\arguments{
\item{filtered_files}{Tibble with filtered file information as produced by filterAndTrimReads()}

\item{n_reads}{Number of reads to use for error learning (default: 1e6)}

\item{pool}{Logical or character indicating whether to pool sequences (default: FALSE)}

\item{min_length}{Minimum acceptable sequence length (default: 0)}

\item{max_length}{Maximum acceptable sequence length (default: Inf)}

\item{remove_chimeras}{Logical indicating whether to remove chimeras (default: TRUE)}

\item{multithread}{Logical or Integer indicating number of threads to use (default: FALSE)}

\item{verbose}{Logical indicating whether to print verbose output (default: TRUE)}

\item{paired}{Logical indicating whether data is paired-end (default: TRUE)}
}
\value{
A list containing the ASV table, ASV sequences, and summary statistics
}
\description{
This function runs the complete DADA2 pipeline from filtered files to ASV table.
}
\examples{
\dontrun{
fastq_files <- detectFastqFiles("path/to/fastq/dir")
filtered_files <- filterAndTrimReads(fastq_files, "path/to/filtered/dir")
results <- runDADA2Pipeline(filtered_files)
}
}
