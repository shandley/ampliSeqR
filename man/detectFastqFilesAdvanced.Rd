% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/file_management.R
\name{detectFastqFilesAdvanced}
\alias{detectFastqFilesAdvanced}
\title{Detect and organize FASTQ files with advanced matching capabilities}
\usage{
detectFastqFilesAdvanced(
  input_dir,
  pattern = ".fastq|.fq",
  paired = TRUE,
  fuzzy_threshold = 0.2,
  extract_metadata = TRUE,
  verify_content = TRUE,
  recursive = TRUE,
  confidence_threshold = 0.7,
  platform = "auto",
  verify_lines = 1000,
  known_patterns = NULL
)
}
\arguments{
\item{input_dir}{Character string specifying the directory containing FASTQ files}

\item{pattern}{Character string specifying a pattern to match in file names (default: ".fastq|.fq")}

\item{paired}{Logical indicating whether to look for paired-end files (default: TRUE)}

\item{fuzzy_threshold}{Numeric value between 0 and 1 indicating the maximum normalized
string distance to consider files as potential matches (default: 0.2)}

\item{extract_metadata}{Logical indicating whether to attempt to extract metadata from
filenames (default: TRUE)}

\item{verify_content}{Logical indicating whether to verify file content (default: TRUE)}

\item{recursive}{Logical indicating whether to search directories recursively (default: TRUE)}

\item{confidence_threshold}{Numeric value between 0 and 1 indicating the minimum confidence
score to include a match in the results (default: 0.7)}

\item{platform}{Character string specifying the sequencing platform, or "auto" to attempt
to detect (options: "auto", "illumina", "pacbio", "nanopore", "iontorrent") (default: "auto")}

\item{verify_lines}{Integer specifying the number of lines to check when verifying content (default: 1000)}

\item{known_patterns}{List containing named elements with platform-specific naming patterns.
If NULL, uses default patterns. (default: NULL)}
}
\value{
A tibble with columns for sample names, forward and reverse read paths, confidence
scores, metadata, and other information
}
\description{
This function provides enhanced file detection with fuzzy matching, metadata extraction,
Bayesian confidence scoring, directory structure analysis, and content verification.
}
\examples{
\dontrun{
# Basic usage with default parameters
fastq_files <- detectFastqFilesAdvanced("path/to/fastq/dir")

# With lower confidence threshold for ambiguous naming
fastq_files <- detectFastqFilesAdvanced("path/to/fastq/dir", confidence_threshold = 0.5)

# For a specific platform with content verification
fastq_files <- detectFastqFilesAdvanced(
  "path/to/fastq/dir", 
  platform = "pacbio", 
  verify_content = TRUE
)
}
}
